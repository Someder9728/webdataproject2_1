# สถาปัตยกรรมและขอบเขตระบบจัดการหอพัก
## Dormitory Management Web Application

เอกสารนี้สรุปจากข้อมูลโครงการ คำตัดสินล่าสุด และโค้ดใน `webdataproject2_1-feature-backend-auth_3.zip` โดยแยกข้อกำหนดของระบบออกจากสถานะการพัฒนา ฟังก์ชันที่อยู่ในขอบเขตไม่ได้หมายความว่าพัฒนาและทดสอบเสร็จแล้วทั้งหมด

เมื่อข้อมูลไม่ตรงกัน ให้ยึดคำตัดสินล่าสุดเป็นหลัก ส่วน ER Model, Proposal และ Figma บางส่วนยังต้องปรับให้สอดคล้องกับข้อกำหนดนี้

## 1. ภาพรวมและเป้าหมาย

โครงการนี้เป็นเว็บแอปพลิเคชันสำหรับบริหารจัดการหอพัก ครอบคลุมข้อมูลผู้เช่า ห้องพัก การเข้าพัก สัญญา ค่าใช้จ่ายรายเดือน การชำระเงิน และการแจ้งซ่อม

ระบบเชื่อมข้อมูลเหล่านี้เข้าด้วยกัน เพื่อให้ผู้ดูแลสามารถดำเนินงานตั้งแต่รับผู้เช่าเข้าพักจนถึงย้ายออก และยังเรียกดูประวัติการเช่า การชำระเงิน และการแจ้งซ่อมย้อนหลังได้

กระบวนการหลักประกอบด้วย:

1. สร้างข้อมูลผู้เช่าและห้องพัก
2. สร้างรายการเช่าที่เชื่อมผู้เช่ากับห้อง
3. จัดเก็บสัญญาภายใต้รายการเช่า
4. บันทึกมิเตอร์น้ำและไฟของห้อง
5. ออกใบแจ้งหนี้ที่ผูกกับรายการเช่า
6. รับและตรวจสอบการชำระเงิน
7. รับแจ้งซ่อมและติดตามการเปลี่ยนสถานะ
8. บันทึกการย้ายออก โดยเก็บประวัติเดิมไว้

การแจ้งซ่อมเป็นกระบวนการที่ดำเนินได้ระหว่างการเข้าพัก ไม่จำเป็นต้องรอให้กระบวนการชำระเงินเสร็จก่อน

## 2. ผู้ใช้งานและขอบเขตสิทธิ์

ระบบมีผู้ใช้งานสองบทบาทหลัก ได้แก่ Admin และ Tenant

### 2.1 Admin — ผู้ดูแลหอพัก

Admin จัดการข้อมูลและกระบวนการของหอพัก ได้แก่:

- ข้อมูลผู้เช่าและห้องพัก
- การเช่าเข้าและการย้ายออก
- รายละเอียดสัญญาเช่า
- การบันทึกมิเตอร์น้ำและไฟ
- ใบแจ้งหนี้และข้อมูลค่าใช้จ่าย
- การตรวจหลักฐานและอนุมัติหรือปฏิเสธการชำระเงิน
- การบันทึกการชำระเงินกรณีผู้เช่าเข้ามาชำระกับผู้ดูแลโดยตรง
- การแจ้งซ่อม การเปลี่ยนสถานะ และประวัติงานซ่อม
- Dashboard การค้นหา และประวัติที่เกี่ยวข้อง

### 2.2 Tenant — ผู้เช่า

Tenant เข้าถึงข้อมูลของตนเอง ได้แก่:

- ข้อมูลส่วนตัว
- ห้องและรายการเช่าของตนเอง
- สัญญาเช่า
- ใบแจ้งหนี้และยอดที่ต้องชำระ
- การส่งหลักฐานการชำระเงิน
- ผลการตรวจสอบและเหตุผลที่ถูกปฏิเสธ
- ประวัติการชำระเงิน
- การแจ้งซ่อมและติดตามรายการของตนเอง

Tenant ต้องไม่สามารถเข้าถึงข้อมูลของผู้เช่าคนอื่นหรือฟังก์ชันจัดการของ Admin ได้ การจำกัดสิทธิ์ต้องตรวจที่ Backend ด้วย ไม่ใช่อาศัยเพียงการซ่อนปุ่มหรือเมนูในหน้าจอ

## 3. โครงสร้างทางเทคนิค

### 3.1 เทคโนโลยีปัจจุบัน

| ส่วน | เทคโนโลยีและสถานะ |
|---|---|
| Backend | PHP และ Laravel |
| โครงโปรเจกต์ปัจจุบัน | Laravel Livewire Starter Kit |
| UI infrastructure ใน ZIP | Blade, Livewire และ Flux |
| Authentication infrastructure | Laravel Authentication และ Fortify |
| Data access | Eloquent ORM |
| Database ปัจจุบัน | SQLite |
| การจัดการ Schema | Laravel Migrations |
| Prototype | Figma Make ใช้อ้างอิงหน้าจอและพฤติกรรม โดยปรับตามข้อกำหนดล่าสุด |
| Database ในอนาคต | Oracle ยังไม่ได้ตัดสินใจแน่นอน |

ไฟล์โครงการระบุ dependency ของ Laravel `^13.17`, Livewire `^4.1` และ PHP `^8.3` ข้อมูลนี้เป็นข้อกำหนดในโปรเจกต์ ไม่ใช่ผลตรวจเวอร์ชันที่ติดตั้งบนเครื่องใช้งานจริง

### 3.2 การแบ่งชั้นความรับผิดชอบ

ระบบแบ่งความรับผิดชอบได้เป็นสี่ชั้น:

| ชั้น | ความรับผิดชอบ |
|---|---|
| Presentation | แสดงหน้าจอ ตาราง ฟอร์ม รายละเอียด สถานะ และรับข้อมูลจากผู้ใช้ |
| Application / Backend | ตรวจการเข้าสู่ระบบ สิทธิ์ ความถูกต้องของข้อมูล และดำเนินกระบวนการของแต่ละ Module |
| Domain / Data access | แทนข้อมูลธุรกิจผ่าน Models และเชื่อมข้อมูลผ่าน Relationships |
| Persistence | จัดเก็บข้อมูลใน SQLite พร้อม Primary Key, Foreign Key, Unique และข้อกำหนดของคอลัมน์ |

เส้นทางข้อมูลตามขอบเขตโครงการคือ:

**ผู้ใช้ → หน้าจอ → Backend → ตรวจสิทธิ์และข้อมูล → Models → Database → ส่งผลกลับหน้าจอ**

ขอบเขตเดิมกำหนดให้มี API สำหรับเชื่อม Frontend กับ Backend แต่ ZIP ปัจจุบันยังเป็นโครง Livewire และยังไม่มี API ธุรกิจครบ จึงยังไม่ถือว่าตัดสินรายละเอียดแล้วว่าหน้าใดใช้ Livewire โดยตรงและหน้าใดเรียก REST API

## 4. Modules ภายในระบบ

### 4.1 Authentication และ Authorization

Module นี้จัดการตัวตนและสิทธิ์ของผู้ใช้ ประกอบด้วย:

- Login และ Logout
- การใช้ `u_username` และ `u_password` เป็นข้อมูลเข้าสู่ระบบ
- Remember Me
- บทบาท `admin` และ `tenant`
- การป้องกัน Route และการกระทำตามสิทธิ์
- การตรวจความเป็นเจ้าของข้อมูลของ Tenant

บัญชีผู้ใช้จัดเก็บในตาราง `users` และเชื่อมกับข้อมูลผู้เช่าผ่าน `tenants_t_id`

โครงสร้าง Laravel ที่จำเป็นสามารถเก็บไว้ได้ แม้ยังไม่มีใน ER เช่น `remember_token` และ timestamps โดย ER ต้องปรับให้สะท้อนการใช้งานจริง

การมีโค้ด Registration, Password Reset, Email Verification, Two-factor Authentication หรือ Passkeys อยู่ใน Starter Kit ยังไม่ถือว่าฟังก์ชันเหล่านี้ได้รับการยืนยันเป็นขอบเขตธุรกิจทั้งหมด

### 4.2 Dashboard

Dashboard สรุปข้อมูลตามบทบาทผู้ใช้งาน

**Admin Dashboard**

- จำนวนห้องทั้งหมด
- จำนวนห้องว่าง
- จำนวนห้องที่มีผู้เช่า
- จำนวนผู้เช่าปัจจุบัน
- ยอดค้างชำระ
- งานซ่อมที่ยังไม่เสร็จ

**Tenant Dashboard**

- ห้องปัจจุบัน
- สถานะการเช่า
- รายละเอียดสัญญา
- ยอดที่ต้องชำระ
- งานซ่อมล่าสุดของตนเอง

Dashboard ใช้ข้อมูลจาก Modules ที่เกี่ยวข้อง โดยไม่ใช่แหล่งจัดเก็บข้อมูลธุรกิจชุดใหม่แยกจากรายการต้นทาง

### 4.3 Tenant Management — จัดการผู้เช่า

จัดเก็บข้อมูลบุคคลที่เป็นผู้เช่า แยกจากบัญชีสำหรับเข้าสู่ระบบ

ข้อมูลหลักประกอบด้วย:

- ชื่อและนามสกุล
- เบอร์โทรศัพท์
- อีเมล
- ที่อยู่

Admin สามารถเพิ่ม แก้ไข ลบ ค้นหา และดูรายละเอียดผู้เช่าได้ โดยการค้นหาครอบคลุมรหัสผู้เช่า ชื่อ และเบอร์โทรศัพท์

หน้ารายละเอียดเชื่อมไปยัง:

- ห้องปัจจุบัน
- ประวัติการเข้าพัก
- ประวัติการชำระเงิน
- ประวัติการแจ้งซ่อม

ความแตกต่างของข้อมูลสองส่วนคือ `users` ใช้ระบุตัวตนและสิทธิ์ในการเข้าใช้งาน ส่วน `tenants` ใช้เก็บข้อมูลผู้เช่าในบริบทของหอพัก

### 4.4 Room Management — จัดการห้องพัก

จัดเก็บข้อมูลห้องพัก ได้แก่:

- เลขหรือชื่อห้อง
- ชั้น
- ประเภทห้อง
- ค่าเช่า
- สถานะห้อง

สถานะหลักที่ยืนยันแล้วคือ **ว่าง** และ **มีผู้เช่า**

Admin สามารถเพิ่ม แก้ไข ลบ ค้นหาห้อง และกรองตามชั้นหรือสถานะได้

หน้ารายละเอียดห้องแสดง:

- ผู้เช่าปัจจุบัน
- ประวัติการเช่า
- ประวัติมิเตอร์
- ประวัติการซ่อมภายในห้อง

สถานะห้องต้องสอดคล้องกับกระบวนการสร้างและสิ้นสุดการเช่า

### 4.5 Rental Management — การเช่าเข้าและย้ายออก

Rental เป็นข้อมูลกลางที่เชื่อม **Tenant กับ Room** และใช้เก็บประวัติการเข้าพักแต่ละครั้ง

ข้อมูลหลักประกอบด้วย:

- ผู้เช่า
- ห้องพัก
- วันที่เข้า
- วันที่ออก
- สถานะการเช่า

**การเช่าเข้า**

เลือกผู้เช่า → เลือกห้องว่าง → ระบุวันที่เข้า → บันทึก Rental → เปลี่ยนห้องเป็นมีผู้เช่า

**การย้ายออก**

เลือก Rental → ระบุวันที่ออก → สิ้นสุดการเช่า → เปลี่ยนห้องเป็นว่าง → เก็บรายการเดิมเป็นประวัติ

ระบบต้องป้องกันการเลือกข้อมูลผู้เช่าและห้องที่ไม่สอดคล้องกัน ในหน้าที่อ้างอิงการเช่าปัจจุบันสามารถเติมข้อมูลที่เกี่ยวข้องให้อัตโนมัติ เช่น เลือกห้องแล้วแสดงผู้เช่าของห้องนั้น

Rental ยังเป็นจุดเชื่อมไปยัง Contract และ Invoices ของการเข้าพักครั้งนั้น

### 4.6 Contract — สัญญาภายใต้การเช่า

Contract เป็นรายละเอียดที่ผูกกับ Rental แบบ **1:1**

ข้อมูลหลักประกอบด้วย:

- เลขที่สัญญา
- วันที่เริ่มและสิ้นสุด
- ค่าเช่าตามสัญญา
- เงินประกัน
- สถานะสัญญา
- Rental ที่เกี่ยวข้อง

หน้าจอเข้าถึงสัญญาต้องอยู่ภายในบริบทของการเช่า เช่น กดแสดงสัญญาจากรายการหรือรายละเอียด Rental

เมนู Contract แบบแยกอิสระใน Figma เป็นข้อมูลเก่า ไม่ใช่รูปแบบหน้าจอที่ยืนยันสำหรับระบบปัจจุบัน อย่างไรก็ตาม Contract ยังมีตารางและ Model แยกเพื่อจัดเก็บรายละเอียดสัญญา

### 4.7 Meter — มิเตอร์น้ำและไฟ

Meter เก็บเลขมิเตอร์ของห้องตามวันที่บันทึก โดย **น้ำและไฟอยู่ใน record เดียวกัน**

ข้อมูลหลัก:

- `m_id`
- `m_date`
- `m_water`
- `m_elec`
- `rooms_r_id`

`m_water` และ `m_elec` เก็บเลขมิเตอร์ ไม่ใช่จำนวนหน่วยที่ใช้ในรอบนั้น

การคำนวณหน่วยใช้:

**หน่วยที่ใช้ = เลขมิเตอร์รอบปัจจุบัน − เลขมิเตอร์รอบก่อน**

ไม่สร้างฟิลด์ Previous ซ้ำใน record ปัจจุบัน โดยใช้ค่าจากประวัติ Meter ของห้องเดียวกันเป็นข้อมูลรอบก่อน

ผลการคำนวณเป็นข้อมูลสำหรับกระบวนการคิดค่าน้ำและค่าไฟ แต่ยังไม่ได้กำหนดตารางหรือวิธีจัดเก็บอัตราต่อหน่วยในข้อมูลที่ได้รับ

### 4.8 Invoice / Billing — ใบแจ้งหนี้

Invoice จัดเก็บค่าใช้จ่ายที่ต้องชำระของ Rental ในแต่ละรอบ

องค์ประกอบหลักคือ:

**ค่าเช่า + ค่าน้ำ + ค่าไฟ = ยอดรวม**

ข้อมูลประกอบด้วย:

- วันที่ใบแจ้งหนี้
- ค่าเช่า
- ค่าน้ำ
- ค่าไฟ
- ยอดรวม
- วันครบกำหนด
- Rental ที่เกี่ยวข้อง

Rental หนึ่งรายการมี Invoice ได้หลายรายการตามระยะเวลาการเข้าพัก

Invoice ไม่เก็บสถานะการชำระซ้ำกับ Payment การแสดงสถานะการชำระต้องอาศัยข้อมูล Payment ที่ผูกอยู่ ส่วนการพิจารณาว่า “เกินกำหนด” เกี่ยวข้องกับวันครบกำหนดและสถานะชำระ โดยรายละเอียดกฎยังต้องกำหนดก่อนพัฒนา

### 4.9 Payment — การชำระเงิน

Payment ผูกกับ Invoice แบบ **1:1** และเป็นผู้จัดการสถานะการชำระเงิน

ข้อมูลหลักในโค้ดปัจจุบัน:

- `p_id`
- `p_date`
- `p_amount`
- `p_type`
- `p_status`
- `p_proof`
- `p_reject_reason`
- `invoices_i_id`

ระบบรองรับสองช่องทาง

**ช่องทางที่ 1: ผู้เช่าส่งหลักฐานผ่านระบบ**

1. ผู้เช่าเปิด Invoice ของตนเอง
2. กรอกข้อมูลการชำระและแนบหลักฐาน
3. ส่งให้ Admin ตรวจสอบ
4. Payment เข้าสถานะ `PENDING`
5. Admin เปิดดูหลักฐานและข้อมูลที่ส่งมา
6. อนุมัติเป็น `PAID` หรือปฏิเสธเป็น `REJECTED`
7. หากปฏิเสธ ต้องมีเหตุผล และผู้เช่าส่งใหม่ได้

สถานะที่ระบุในข้อกำหนด ได้แก่ `UNPAID`, `PENDING`, `PAID` และ `REJECTED`

**ช่องทางที่ 2: Walk-in**

ผู้เช่าเข้ามาชำระกับผู้ดูแลโดยตรงนอกระบบ จากนั้น Admin บันทึกการชำระให้ Invoice ผ่านระบบได้ด้วยตนเอง

ดังนั้น Admin ต้องมีความสามารถทั้งตรวจการส่งหลักฐานและบันทึกการชำระโดยตรง รายละเอียดฟิลด์บังคับของช่องทาง Walk-in ยังไม่ได้กำหนดครบ

ไม่มีการเชื่อม Payment Gateway เพื่อรับเงินออนไลน์โดยตรงในขอบเขตนี้

### 4.10 Repair — การแจ้งซ่อม

Repair เป็น Module เดียว แบ่งประเภทเป็น:

| ประเภท | ความหมาย | ห้องที่อ้างอิง |
|---|---|---|
| `ROOM` | ปัญหาภายในห้องพัก | ต้องมี `rooms_r_id` |
| `COMMON` | ปัญหาพื้นที่ส่วนกลาง | `rooms_r_id` ต้องเป็น `NULL` |

ข้อมูลหลักประกอบด้วย:

- หัวข้อปัญหา `rp_name`
- รายละเอียด `rp_description`
- ประเภท `rp_type`
- สถานะ `rp_status`
- ผู้เช่าที่เกี่ยวข้อง `tenants_t_id`
- ห้อง `rooms_r_id` เมื่อเป็นงานภายในห้อง

กรณี Common Repair ให้ใส่ตำแหน่งและรายละเอียดใน `rp_name` หรือ `rp_description` เดิม **ไม่เพิ่มคอลัมน์ `location`**

ลำดับสถานะคือ:

**แจ้งแล้ว → กำลังซ่อม → เสร็จแล้ว**

ผู้เช่าดูและติดตามรายการของตนเอง ส่วน Admin จัดการงานและเปลี่ยนสถานะได้

### 4.11 Repair History — ประวัติงานซ่อม

ทุกครั้งที่เปลี่ยนสถานะการซ่อม ต้องบันทึกประวัติใน `repair_histories`

Repair หนึ่งรายการมีประวัติได้หลายรายการ โดยข้อมูลประวัติประกอบด้วยสถานะ ข้อมูลอธิบาย และเวลาที่เกี่ยวข้องตาม Schema

โครงสร้างนี้ใช้แสดง Timeline ของงานซ่อมและติดตามการดำเนินงานย้อนหลัง ไม่ควรมีเพียงการเขียนทับสถานะปัจจุบันโดยไม่มีประวัติ

### 4.12 Search และ History

ไม่มีเมนู Global Search / History แยกเป็น Module กลาง

การค้นหาอยู่ในแต่ละ Module:

| Module | ขอบเขตค้นหาที่ระบุไว้ |
|---|---|
| Tenant | รหัส ชื่อ เบอร์โทร |
| Room | เลขห้อง พร้อมกรองชั้นและสถานะ |
| Rental | ผู้เช่าและห้อง |
| Invoice | ผู้เช่า ห้อง และรอบบิล |
| Repair | ห้อง ผู้แจ้ง และหัวข้อ |

ประวัติแสดงในหน้ารายละเอียดของข้อมูลที่เกี่ยวข้อง เช่น Tenant, Room, Rental, Payment และ Repair

## 5. สถาปัตยกรรมข้อมูล

### 5.1 ตารางธุรกิจหลัก

ระบบมี **10 ตารางหลัก** ไม่รวมตารางสนับสนุนของ Laravel

| ตาราง | Primary Key | หน้าที่ |
|---|---|---|
| `users` | `u_id` | บัญชีผู้ใช้และบทบาท |
| `tenants` | `t_id` | ข้อมูลผู้เช่า |
| `rooms` | `r_id` | ข้อมูลห้อง |
| `rentals` | `rt_id` | การเข้าพักและความเชื่อมโยงผู้เช่ากับห้อง |
| `contracts` | `c_id` | สัญญาของ Rental |
| `meters` | `m_id` | เลขมิเตอร์น้ำและไฟของห้อง |
| `invoices` | `i_id` | ค่าใช้จ่ายของ Rental |
| `payments` | `p_id` | การชำระและผลตรวจสอบ |
| `repairs` | `rp_id` | รายการแจ้งซ่อม |
| `repair_histories` | `rph_id` | ประวัติการดำเนินงานซ่อม |

Migration ปัจจุบันใช้ ID แบบตัวเลขเพิ่มอัตโนมัติ และแต่ละ Model กำหนดชื่อ Primary Key ให้ตรงกับตารางแล้ว จึงไม่ใช้ชนิด `CHAR(8)` จาก ER เก่าเป็นข้อกำหนดสำหรับโค้ดปัจจุบัน

### 5.2 ความสัมพันธ์หลัก

| ความสัมพันธ์ | รูปแบบ | Foreign Key |
|---|---|---|
| Tenant → User | ไม่เกินหนึ่งบัญชีต่อผู้เช่า | `users.tenants_t_id` |
| Tenant → Rental | 1:N | `rentals.tenants_t_id` |
| Room → Rental | 1:N สำหรับประวัติการเช่า | `rentals.rooms_r_id` |
| Rental → Contract | 1:1 | `contracts.rentals_rt_id` |
| Room → Meter | 1:N | `meters.rooms_r_id` |
| Rental → Invoice | 1:N | `invoices.rentals_rt_id` |
| Invoice → Payment | 1:1 | `payments.invoices_i_id` |
| Tenant → Repair | 1:N | `repairs.tenants_t_id` |
| Room → Repair | 1:N โดย Repair บางรายการไม่มีห้อง | `repairs.rooms_r_id` |
| Repair → RepairHistory | 1:N | `repair_histories.repairs_rp_id` |

Foreign Key ของ User–Tenant, Contract–Rental และ Payment–Invoice มี UNIQUE ใน Migration แล้ว

ในระดับฐานข้อมูล UNIQUE บังคับว่าไม่สามารถมีหลายรายการลูกอ้างอิงรายการแม่เดียวกัน แต่ไม่ได้บังคับให้รายการแม่ต้องมีรายการลูกทันที เช่น Invoice อาจยังไม่มี Payment จนกว่าจะมีขั้นตอนสร้างรายการนั้น ซึ่งจังหวะการสร้างต้องกำหนดใน Backend

### 5.3 ข้อมูลเวลาและการลบ

ตารางธุรกิจหลักใช้:

- `created_at`
- `updated_at`
- `deleted_at`

Models ทั้ง 10 ตัวรองรับ Soft Delete แล้วใน ZIP ล่าสุด

Soft Delete ทำให้เก็บ record เดิมไว้ได้ แต่การเลือกว่าจะอนุญาตให้ลบข้อมูลใดในสถานะใด และการแสดงข้อมูลที่ถูกลบในหน้าประวัติ ยังต้องกำหนดให้สัมพันธ์กับกระบวนการธุรกิจ การย้ายออกใช้การสิ้นสุด Rental ไม่ใช่การลบทิ้ง

### 5.4 ตารางสนับสนุนของ Framework

โปรเจกต์ยังมีตารางสนับสนุน เช่น Sessions, Password Reset Tokens, Cache, Jobs และ Passkeys รวมถึงคอลัมน์ Two-factor Authentication ใน User

ส่วนเหล่านี้เป็นโครงสร้างทางเทคนิคที่แยกจาก 10 ตารางธุรกิจหลัก การคงไว้ต้องปรับให้เข้ากับ User Schema ปัจจุบัน และไม่หมายถึงเพิ่มฟังก์ชันธุรกิจใหม่โดยอัตโนมัติ

## 6. กฎความถูกต้องและจุดเชื่อมระหว่าง Modules

Backend ต้องรักษาความสอดคล้องของข้อมูลในกระบวนการต่อไปนี้:

| กระบวนการ | ความสอดคล้องที่ต้องรักษา |
|---|---|
| สร้าง Rental | ผู้เช่าและห้องถูกต้อง ห้องพร้อมให้เช่า และสถานะห้องเปลี่ยนตาม |
| ย้ายออก | Rental สิ้นสุด ห้องกลับเป็นว่าง และประวัติยังอยู่ |
| จัดการ Contract | Contract อ้างอิง Rental ที่ถูกต้องและไม่ซ้ำ |
| บันทึก Meter | ใช้ข้อมูลห้องและลำดับการอ่านมิเตอร์ที่ถูกต้อง |
| ออก Invoice | ใบแจ้งหนี้ผูก Rental ที่ถูกต้องและยอดรวมตรงกับองค์ประกอบ |
| ส่ง Payment | Tenant ส่งข้อมูลให้ Invoice ของตนเองเท่านั้น |
| ตรวจ Payment | Admin เป็นผู้อนุมัติหรือปฏิเสธ และการปฏิเสธมีเหตุผล |
| บันทึก Walk-in | Admin บันทึกให้ Invoice ที่ถูกต้อง |
| สร้าง Repair | ประเภท `ROOM` และ `COMMON` สอดคล้องกับค่าห้อง |
| เปลี่ยนสถานะ Repair | สถานะปัจจุบันและประวัติการเปลี่ยนสถานะสอดคล้องกัน |

Validation ครอบคลุมข้อมูลจำเป็น ความไม่ซ้ำ วันที่ ตัวเลข และความถูกต้องของความสัมพันธ์

สำหรับการอ่านข้อมูล ต้องระวังการ JOIN ความสัมพันธ์แบบ 1:N หลายชุดพร้อมกัน เพราะอาจทำให้รายการซ้ำและยอดรวมผิด เช่น รวมประวัติ Rental, Payment และ Repair ของ Tenant ในผลลัพธ์เดียวโดยไม่แยกการรวมข้อมูลให้เหมาะสม

## 7. ขอบเขตที่ไม่รวม

สิ่งต่อไปนี้อยู่นอกขอบเขตที่ยืนยัน:

- Payment Gateway จริง
- ระบบ Chat
- ระบบ Real-time
- การเชื่อมต่อระบบภายนอกที่ซับซ้อน

Oracle ยังเป็นเพียงประเด็นที่อาจพิจารณาภายหลัง ปัจจุบันใช้ SQLite เป็นหลัก

## 8. รายละเอียดที่ยังไม่ควรกำหนดแทนทีม

เพื่อให้ Architecture ไม่เพิ่มข้อกำหนดที่ยังไม่ได้ตกลง ประเด็นต่อไปนี้ยังต้องระบุรายละเอียดก่อนพัฒนาส่วนที่เกี่ยวข้อง:

| ประเด็น | สิ่งที่ยังไม่ยืนยัน |
|---|---|
| Frontend–Backend | ขอบเขตการใช้ Livewire เทียบกับ REST API และรูปแบบ API |
| วงจรบัญชีผู้ใช้ | ใครสร้างบัญชี Tenant และใช้ฟังก์ชันเสริมของ Fortify ใดจริง |
| Rental | จำนวนการเช่าที่เปิดพร้อมกันต่อผู้เช่า และรายละเอียดกรณีเปลี่ยนห้อง |
| Contract | เวลาที่สร้างสัญญาและกฎการสิ้นสุดหรือเปลี่ยนแปลงสัญญา |
| Meter | ค่าตั้งต้นครั้งแรก มิเตอร์เปลี่ยนใหม่ เลขย้อนกลับ และการแบ่งรอบเมื่อย้ายเข้า–ออก |
| Billing | อัตราน้ำ–ไฟ แหล่งเก็บอัตรา การปัดเศษ และการคิดค่าเช่าไม่เต็มเดือน |
| Payment | จังหวะสร้าง `UNPAID` ข้อมูลบังคับของ Walk-in และวิธีเก็บหลักฐานเมื่อส่งใหม่ |
| Overdue | วิธีแสดงและคำนวณยอดเกินกำหนดร่วมกับสถานะ Payment |
| Repair | การระบุผู้แจ้งเมื่อ Admin สร้างรายการเอง เพราะ Schema ปัจจุบันบังคับ `tenants_t_id` |
| การลบและประวัติ | ข้อจำกัดการลบข้อมูลที่ยังถูกใช้งาน และวิธีแสดงข้อมูลที่ Soft Delete แล้ว |

## 9. สถานะพื้นฐานที่ยืนยันจากโค้ดล่าสุด

ZIP รอบ 3 มี Migration และ Models ของข้อมูลหลัก พร้อมการแก้ไขที่ตรวจแล้ว ได้แก่:

- ชื่อ Primary Key ของ Models
- Soft Delete
- ชื่อ FK `tenants_t_id`
- Imports ของ Invoice
- ความสัมพันธ์ Meter–Room
- การย้อน Migration ของ Meter
- FK ของ Passkeys ที่อ้างอิง `users.u_id`

อย่างไรก็ตาม ยังไม่มีผลทดสอบฐานข้อมูลจริงหรือ Relationships ในข้อมูลที่ส่งมา และ Authentication ยังต้องปรับจากรูปแบบ Starter Kit ให้ตรงกับ User Schema

จึงใช้โค้ดชุดนี้เป็นฐานการพัฒนาด้านข้อมูลได้ โดยสถานะปัจจุบันคือ **ตรวจการแก้ไขในโค้ดแล้ว แต่ยังต้องตรวจ Migration ที่ใช้งานจริงและทดสอบกระบวนการของระบบ** ก่อนยืนยันว่าฟังก์ชันทำงานครบตาม Architecture นี้